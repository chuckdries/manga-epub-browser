use std::env;

use crate::{AppError, AppResponse};
// This filter does not have extra arguments
// pub fn mangathumb<T: std::fmt::Display>(s: T) -> Result<String, ::askama::Error> {
//     let s = format!("{}/{}", &env::var("SUWAYOMI_URL").ok()?, s);
//     Ok(s)
// }
